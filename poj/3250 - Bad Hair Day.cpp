#include <iostream>
#include <cstring>
#include <stack>
using namespace std;

int main() {
  int n;
  cin >> n;
  int height[n];
  for (int i = 0; i < n; ++i) {
    cin >> height[i];
  }
  int can_see = 0;
  for (int i = 0; i < n; ++i) {
    stack<int> s;
    for (int j = i; j < n; ++j) {
      if (s.empty())
    }
  }
}
